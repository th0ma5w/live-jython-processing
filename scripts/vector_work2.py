for x in b:
    #x.position.x=float(width)/float(len(b)) * b.index(x)
    #x.position.y = height/2
    x.position=PVector(width/2,height/2)
    x.velocity=PVector(0,0)
    x.size=random(50)
    x.topspeed=500


​
